name: app
description: Projeto Rock in Rio
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_native_splash: ^2.4.0 # 👈 AQUI é dependência de desenvolvimento

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png
    - assets/images/bg.png

# 👇 CONFIGURAÇÃO DA SPLASH NATIVA
flutter_native_splash:
  color: "#000000"

  android_12:
    color: "#000000"

  fullscreen: true
